import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles({
  paper: {
    padding: "23px 20px",
    width: 1000,
  },
  button: {
    marginTop: 30,
    backgroundColor: "#9be4e8",
  },
  paramContainer: {
    marginTop: 20,
  },
});