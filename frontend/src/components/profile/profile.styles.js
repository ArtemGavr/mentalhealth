import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles({
  paper: {
    padding: "23px 20px",
    width: 375,
    margin: "10px auto",
  },
  paramContainer: {
    marginTop: 20,
  },
  editButton: {
      width: "100"
  }
});